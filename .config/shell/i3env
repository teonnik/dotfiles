# vim: filetype=sh

export XDG_CURRENT_DESKTOP=i3
export XDG_SESSION_DESKTOP=i3
export XDG_SESSION_TYPE=x11

export LPASS_CLIPBOARD_COMMAND="xclip -selection clipboard -in -l 1"

# start a X session for i3
startx
